---
phase: 2
title: "Dispatch Tool — meta_state_dispatch_finding"
status: pending
priority: P2
dependencies: [1]
---

# Phase 2: Dispatch Tool — meta_state_dispatch_finding

## Overview
Add a new MCP tool `meta_state_dispatch_finding` with two modes — **prepare** and **commit** — that coordinate with the agent to route a fixable finding to a GitHub Issue. The **core tool does not access GitHub** (two-surfaces: the deterministic core does no external side effects); the **agent** runs `gh issue create` as a Bash command. The tool prepares the issue body (prepare), and after the agent creates the issue, records the `dispatch-<finding_id>` ledger event + patches the finding's `ledger_ref` back-pointer (commit). Idempotent at both modes via a ledger scan. Operator-gated on commit; prepare is read-only/ungated. One-way derivation: finding → issue → resolve-on-close.

## Requirements
- Functional: `prepare({id})` returns the issue title/body + citation, refusing (returning existing coords) if a `dispatch-<id>` ledger row already exists. `commit({id, issue_number, issue_url, repo})` writes the ledger event (via shared `appendLedgerEvent` helper) + patches `ledger_ref` to `dispatch-<id>` (CAS), refusing if `ledger_ref` is set or a `dispatch-<id>` row exists with DIFFERENT coords (returns existing coords). The issue body cites `local:meta-state:<id>`.
- Non-functional: **no `gh` spawn in the core tool** (the agent runs `gh`); idempotent via ledger scan (handles orphan-retry AND concurrent races); CAS-protected patch (`_expected_version`); commit is operator-gated (`OPERATOR_MODE`); `gh` failure handling is an agent-side concern (the agent checks `gh`'s exit code before calling commit); the shared `appendLedgerEvent` helper is extracted from `runtime-state-record-tool.js:59-76` (DRY).

## Architecture
**Two-surfaces split (per operator correction 2026-07-04):** the deterministic core (MCP tools) does no external side effects; the agentic runtime (the agent) runs `gh`. So the dispatch is a **protocol** between the agent and the tool, not a single spawning tool:

1. **Agent calls `meta_state_dispatch_finding({id, stage:"prepare"})`.** The tool validates the finding, scans `runtime-state.jsonl` for an existing `dispatch-<id>` row (idempotency), and if none, builds the issue title + body (id, category, severity, affected_system, evidence_code_ref [repo-relative, no redaction], description, `local:meta-state:<id>` citation). Returns `{finding_id, issue_title, issue_body, coord_repo_hint}` where `coord_repo_hint` is **advisory text only** (no env-var default, no allowlist, no content-gate — per INC-1 reversal). Read-only — no write, ungated.
2. **Agent runs `gh issue create --json number,url --title "<title>" --body "<body>"`** as a Bash command. The agent may add `--repo <coord-repo>` to target a specific repo; if `--repo` is omitted, `gh` defaults to the current git remote (THIS repo). The bash gate (`hooks/legacy/bash-gate.js:27`) intercepts the agent's Bash tool; `gh` is NOT in `core/patterns.json` constraints (`docker|sudo|package-manager|vendor-api|side-effect-import`) → returns `ok` (passes). The agent checks `gh`'s exit code + stdout before proceeding (agent-side failure handling).
3. **Agent calls `meta_state_dispatch_finding({id, stage:"commit", issue_number, issue_url, repo})`.** The tool re-checks idempotency (ledger scan), writes the `dispatch-<id>` ledger event (via `appendLedgerEvent`) holding `{issue_number, issue_url, repo, dispatched_by, dispatched_at, finding_id, delegated_to}`, and patches the finding's `ledger_ref` to `dispatch-<id>` (CAS via `updateEntry`). The `repo` field on commit captures whatever the agent passed to `gh` (or empty if the agent used `gh`'s default). The MCP tool does NOT validate or default this field — coord-repo policy is procedural, not tool-level.

**Idempotency (handles H1 orphan-retry + H2 concurrent-race):** both modes scan `runtime-state.jsonl` for a row with `id === "dispatch-<finding_id>"` BEFORE acting. Prepare: if found, return its `metadata.issue_number`/`issue_url` (no re-prepare, agent does NOT re-run `gh`). Commit: if a row exists with the SAME coords → no-op success; if a row exists with DIFFERENT coords → refuse (`already_dispatched`, return existing coords); only if NO row → write + patch. `runtime-state.jsonl` is append-only with no uniqueness check (scout confirmed `runtime-state-record-tool.js:76` `appendFileSync`) — so the ledger scan is the dedupe, not the id.

**INC-1 reversal (Q3 + brainstorm Addendum 3 confirmation):** The red-team P2 F8 added `LOOP_DISPATCH_REPO` env var, `LOOP_DISPATCH_ALLOWED_REPOS` allowlist, `LOOP_DISPATCH_REQUIRE_PRIVATE` content-gate, and `LOOP_DISPATCH_FORCE_PUBLIC=1` bypass to the deterministic core. **This was a misread of the two-surfaces split.** The brainstorm's Addendum 3 (`plans/reports/brainstorm-260704-...md:63,190`) explicitly rejected tool-level gates: "the disclosure mitigation is procedural (private coord repo + operator-edited description), not tool-level." All four env vars + the content-gate + the bypass are dropped. Coord-repo policy lives in the Rec 10 surfacing prompt + operator judgment.

**P2 F7 fix (concurrent-race test):** the test in step 8 uses `Promise.all([commit(), commit()])` (true concurrency, not sequence) to assert that the first-write-wins via CAS holds under real concurrent pressure. Acceptance: either ONE ledger row + BOTH commits succeed (one found existing row, returned `already_dispatched` or no-op), OR ONE ledger row + ONE commit succeeds + ONE returns `version_mismatch`. The test asserts `sweepText.results` (or equivalent) shows exactly ONE `applied: true` and the rest either `no-op` or `version_mismatch`. **Renamed:** "concurrent-dispatch test" (not "concurrent-race" — that name is misleading). The CAS layer is the only safety under true concurrency; the ledger scan is best-effort.

**Disclosure mitigation (procedural only):** the Rec 10 surfacing prompt (Phase 3) instructs: "dispatch to a **private coordination repo**; agent proposes, operator dispatches." The operator's authority (R3) is the boundary — the agent proposes, the operator picks the repo + authorizes. If dispatch-to-public is ever enabled, the operator edits the description to a non-exploitable summary before create (content gate, not a path-redaction step; `evidence_code_ref` is repo-relative and already as public as the tree). **No tool-level enforcement** (per Addendum 3 + INC-1 reversal).

**`ledger_ref`** (finding schema `core/meta-state.js:108`, patchable — NOT in `IMMUTABLE_PATCH_FIELDS` `:274-285`) is set to the bare ledger-event id `dispatch-<finding_id>` via `updateEntry(root, finding_id, { ledger_ref, _expected_version: version })` (CAS at `:573-577`).

**Citation asymmetry (P3 F10):** the issue body cites `local:meta-state:<finding_id>` — a loop-citable pointer for agents; non-loop readers (humans) cannot click through. The inverse — the issue URL cited back into the finding's `ledger_ref` (set to `dispatch-<finding_id>`) — is queryable by humans via `gh issue view <n>` but NOT back-linkable from the issue side. The two are NOT symmetric: the body-side citation is for agents (loop-citable); the registry-side back-pointer is for humans (URL-citable). Document this in the issue body template so future agents understand.

**Orthogonal-gate design (P2 F6 fix):** two distinct enforcement surfaces — `runtime_state_record` is **preflight-gated** (30-min `.loop-preflight-*` marker per surface; checked at `runtime-state-record-tool.js:50-57` via `hasPreflightMarker(root)`). The dispatch tool's commit is **`OPERATOR_MODE`-gated** (env-var `=== "1" || "true"`; checked by the tool's outer guard). These are intentional, orthogonal gates. `appendLedgerEvent` (the extracted helper) does **NOT** check preflight — that check stays at the public-tool boundary. The dispatch tool checks `OPERATOR_MODE` FIRST, then calls `appendLedgerEvent` (which only does the append + fingerprint). A preflight-installed non-operator agent calling commit is refused at the `OPERATOR_MODE` guard, before the helper is invoked. Add a test asserting: agent WITH preflight marker + WITHOUT `OPERATOR_MODE=1` → refused (`operator_role_required`); agent WITHOUT preflight marker + WITH `OPERATOR_MODE=1` → succeeds (the dispatch tool bypasses the preflight check).

**Orphan self-heal (INC-10):** if `updateEntry` fails after the ledger event is written (both retries), the ledger row is orphaned (issue exists, no back-pointer). **Self-healing requires the operator or agent to re-invoke the tool** (next prepare or commit with the same `id`). The next prepare/commit scan finds the `dispatch-<id>` row and returns its coords — so the agent re-commits with the same coords, the no-op path fires, and `ledger_ref` gets patched on retry. **No proactive orphan-detection scan exists**; the operator/agent must trigger the re-invocation. Phase 3's Rec 10 surfacing filters out dispatched (`ledger_ref`-set) findings, so orphans (with NO `ledger_ref`) ARE re-surfaced — this provides a natural operator awareness path. Document the trigger explicitly in the Rec 10 surfacing prompt ("dispatched-but-no-ledger_ref findings are orphans; re-invoke the dispatch tool to heal").

## Related Code Files
- Create: `tools/learning-loop-mastra/tools/legacy/meta-state-dispatch-finding-tool.js` — export `metaStateDispatchFindingTool`; template `meta-state-ack-tool.js`. One tool, `stage` param (`"prepare"|"commit"`). Return shape `{ content: [{ type:"text", text: JSON.stringify(result) }] }` (unwrapped by `legacy-handler-adapter.js:12-26`).
- Modify: `tools/learning-loop-mastra/tools/manifest.json` — add one entry: `{ "file": "tools/meta-state-dispatch-finding-tool.js", "export": "metaStateDispatchFindingTool", "pathFields": [] }`. JSONC: full-line `//` comments only, **no trailing commas**.
- **Q1 fix:** Modify `tools/learning-loop-mastra/agent-manifest.json:19` — add `"mastra_meta_state_dispatch_finding"` to the `tools` array. Required for the agent to discover the tool via the manifest surface.
- Modify (shared helper): extract `appendLedgerEvent(root, entry)` from `tools/legacy/runtime-state-record-tool.js:59-76` (append + fingerprint path) into `core/runtime-state.js` (or a new `core/ledger.js`); call from both the record tool and the dispatch commit step. The helper does NOT check preflight (the public-tool boundary keeps the `hasPreflightMarker(root)` check — orthogonal-gate design, P2 F6).
- Read-only: `tools/learning-loop-mastra/core/meta-state.js` (`updateEntry` `:573-599`, `IMMUTABLE_PATCH_FIELDS` `:274-285`, finding `ledger_ref` `:108`).
- Read-only: `tools/learning-loop-mastra/core/patterns.json` (no `gh` constraint — confirmed; the agent's `gh` call passes the bash gate). Do NOT route `gh` through `core/verification-runner.js` (its `VERIFY_ALLOWLIST` excludes `gh`).
- Create: tests — prepare builds body + idempotent-returns-existing-coords; commit writes ledger + patches `ledger_ref` + refuses duplicate coords; non-operator commit refused; **P2 F6 — orthogonal-gate tests** (with-preflight-without-OPERATOR refused; without-preflight-with-OPERATOR succeeds); **P2 F7 — concurrent-dispatch test (renamed)** uses `Promise.all` to assert CAS safety.

## Implementation Steps (TDD — tests first)
1. **Test first (red):** prepare test — `meta_state_dispatch_finding({id, stage:"prepare"})` on an undispatched finding returns `{finding_id, issue_title, issue_body, coord_repo_hint}` with the body containing `local:meta-state:<id>`; no ledger row, no `ledger_ref` change (read-only). Run; fails (no tool).
2. **Create the tool + manifest entries.** No `server.js` edit (dynamic import at `server.js:44-60`). Schema: `id` (required), `stage` (enum `prepare|commit`, default `prepare`), and **commit-only** params `issue_number` (number), `issue_url` (string), `repo` (string, optional, captures whatever the agent passed to `gh`), optional `delegated_to`. **INC-2 fix:** `repo` is commit-only (not prepare-accepted) — confirms the schema; the gate-test contradiction is moot after INC-1 reversal (no prepare-side gate exists). Also add the manifest entry to `agent-manifest.json:19` (Q1) so the agent discovers the tool.
3. **Prepare handler:** validate finding exists + is a finding; scan `runtime-state.jsonl` for `id === "dispatch-<id>"` — if found, return `{dispatched:false, reason:"already_dispatched", issue_number:<from metadata>, issue_url:<from metadata>}` (idempotent — agent does NOT re-run `gh`); else build the title/body (with body footer noting the citation asymmetry — see Architecture) and return `{finding_id, issue_title, issue_body, coord_repo_hint:"operator-names-repo-at-gh-time"}`. **`coord_repo_hint` is advisory text only** — no env var, no allowlist, no content-gate. Read-only, ungated.
4. **Test first (red):** commit test — `meta_state_dispatch_finding({id, stage:"commit", issue_number:42, issue_url:"...", repo:"..."})` writes a `dispatch-<id>` ledger event (via `appendLedgerEvent`) holding the coords + `delegated_to`, and patches the finding's `ledger_ref` to `dispatch-<id>` (CAS). Run; fails.
5. **Extract `appendLedgerEvent` helper** from `runtime-state-record-tool.js:59-76` into `core/runtime-state.js` (append + `fingerprint` = `sha256:` over `id|source_ref|value|delta|timestamp` + `status:"active"`). The helper does NOT enforce preflight (preflight check stays at the public-tool boundary of `runtime_state_record`). Refactor `runtime_state_record` to call it. Add a regression test that the record tool still works.
6. **Commit handler:** operator-gate (`OPERATOR_MODE === "1" || "true"`; else `{dispatched:false, reason:"operator_role_required"}`). Re-scan the ledger for `dispatch-<id>`: if a row exists with the SAME coords → no-op success; if with DIFFERENT coords → `{dispatched:false, reason:"already_dispatched", existing_issue_number, existing_issue_url}` (refuse duplicate); if NO row → `appendLedgerEvent({kind:"ledger-event", affected_system:"meta-state-tools", id:"dispatch-<id>", source_ref:"local:meta-state:<id>", value:null, delta:null, timestamp, metadata:{issue_number, issue_url, repo, dispatched_by, dispatched_at, finding_id, delegated_to}})`. Then read the finding's `version` and `updateEntry(root, id, { ledger_ref:"dispatch-<id>", _expected_version: version })`; on `version_mismatch`, retry once with a fresh version.
7. **Test first (red):** idempotency tests — (a) re-prepare after a ledger row exists returns existing coords (no new body build); (b) re-commit with same coords → no-op; (c) commit with different coords after a row exists → refused, returns existing; (d) non-operator commit → `operator_role_required`. Run → green.
8. **Test first (red):** **P2 F6 fix — orthogonal-gate tests:** (e) commit with preflight marker present + WITHOUT `OPERATOR_MODE=1` → refused (`operator_role_required`); (f) commit WITHOUT preflight + WITH `OPERATOR_MODE=1` → succeeds (dispatch bypasses preflight; orthogonal-gate design). Run → green.
9. **Test first (red):** **P2 F7 fix — concurrent-dispatch test (renamed from "concurrent-race"):** `Promise.all([commit(), commit()])` with same `id` + same coords. Acceptance: ONE ledger row + ONE `applied: true` + the other returns either `no-op` (same coords case) OR `version_mismatch` (different coords case). Verify via the returned `results` array (or equivalent) — `successes + no_ops + mismatches = 2`. Documents that CAS is the only safety under true concurrency; the ledger scan is best-effort. Run → green.
10. **INC-10 — Orphan self-heal documentation in Rec 10 surfacing prompt:** document the explicit trigger — "dispatched-but-no-ledger_ref findings are orphans; the operator or agent must re-invoke the dispatch tool to heal." Phase 3's Rec 10 surfacing filter (`severity !== "escalate"`, no `ledger_ref`, non-terminal) **already surfaces orphans** (since they have no `ledger_ref`); the prompt must explain the operator action. Update Phase 3's surfacing builder to include an orphan-status hint alongside the regular "fixable stale dispatch candidates" output.
11. **Verify:** `pnpm test` green; manifest JSONC valid (no trailing comma); `agent-manifest.json` includes `mastra_meta_state_dispatch_finding`; tool works end-to-end. Manual: agent prepare → `gh issue create` (with or without `--repo`) → commit; `gh issue view <n> --json` confirms the body citation and the finding's `ledger_ref` resolves to the ledger event.

## Success Criteria
- [ ] `prepare({id})` builds the issue body with `local:meta-state:<id>`; read-only (no ledger/`ledger_ref` change); idempotent (returns existing coords if `dispatch-<id>` row exists); `coord_repo_hint` is advisory text only (INC-1 reversal — no env var, no allowlist, no content-gate).
- [ ] `commit({id, issue_number, issue_url, repo})` writes the `dispatch-<id>` ledger event (via `appendLedgerEvent`) + patches `ledger_ref` (CAS); operator-gated. `repo` is **commit-only** (INC-2 fix).
- [ ] **P2 F6 — Orthogonal-gate tests:** agent WITH preflight + WITHOUT `OPERATOR_MODE=1` → refused (`operator_role_required`); agent WITHOUT preflight + WITH `OPERATOR_MODE=1` → succeeds (the dispatch tool bypasses preflight by design — `appendLedgerEvent` does NOT check preflight).
- [ ] Re-commit with same coords → no-op; with different coords → refused, returns existing (idempotent via ledger scan).
- [ ] **P2 F7 — Concurrent-dispatch test (renamed):** `Promise.all([commit(), commit()])` with same id → ONE ledger row, ONE `ledger_ref`; the other commit returns `no-op` or `version_mismatch`. Documents CAS as the only safety under true concurrency.
- [ ] **Q1 — `agent-manifest.json:19` updated:** `mastra_meta_state_dispatch_finding` is in the tools array; agent discovers the tool via the manifest surface.
- [ ] Non-operator commit → `operator_role_required`; prepare is ungated.
- [ ] **No `gh` spawn in the core tool**; the agent runs `gh issue create` (bash-gated, passes).
- [ ] Shared `appendLedgerEvent` helper extracted; `runtime_state_record` still works (regression test green).
- [ ] Orphan self-heal documented (INC-10): Phase 3 Rec 10 surfacing prompt explains that orphan findings (no `ledger_ref` after dispatch) require re-invocation; the surfacing filter already surfaces them since they have no `ledger_ref`.
- [ ] **P3 F10 — Issue body footer** notes the citation asymmetry: `local:meta-state:<id>` is loop-citable; `gh issue view <n>` is human-citable; they are NOT symmetric.
- [ ] `pnpm test` green; manifest JSONC valid.

## Risk Assessment
- **High — duplicate dispatch via orphan-retry or true concurrent race.** Mitigation: both modes scan the ledger for `dispatch-<id>` BEFORE acting; prepare returns existing coords (agent does not re-run `gh`); commit refuses duplicate coords. The ledger scan is best-effort; CAS on `ledger_ref` is the only safety under true concurrency. The P2 F7 test verifies this with `Promise.all`.
- **High — `gh` failure handling is agent-side.** The tool does not spawn `gh`, so the agent must check `gh`'s exit code + stdout before calling commit. Mitigation: the Rec 10 surfacing prompt (Phase 3) instructs the agent protocol (prepare → gh [check exit code] → commit); if `gh` fails, the agent does NOT call commit (no orphan). Document this in the prompt.
- **Medium — disclosure (issue created in the public template repo).** Mitigation: **procedural only (INC-1 reversal — Q3).** The Rec 10 surfacing prompt instructs "private coordination repo; agent proposes, operator dispatches"; the operator names the repo at dispatch time. No tool-level enforcement (per brainstorm Addendum 3 + the plan's two-surfaces split). No `evidence_code_ref` redaction (repo-relative, already as public as the tree). If dispatch-to-public is ever enabled, the operator edits the description to a non-exploitable summary (content gate).
- **Medium — `ledger_ref` value shape.** Bare id `dispatch-<finding_id>` (deterministic; join by id lookup in `runtime-state.jsonl`). The ledger event's `source_ref` is `local:meta-state:<finding_id>` (inverse pointer, regex `^local:meta-state:.+$`).
- **Medium — P2 F6 gate-confusion risk.** `runtime_state_record` is preflight-gated; the dispatch tool bypasses preflight by design. A preflight-installed non-operator agent could be confused about which gate applies. Mitigation: orthogonal-gate tests in step 8 assert both sides; the Architecture section above documents the gate split.
- **Low — orphan self-heal requires re-invocation (INC-10).** If the agent that created the orphan crashes, no one re-invokes the tool. Phase 3's Rec 10 surfacing filters out dispatched (`ledger_ref`-set) findings, so orphans (with NO `ledger_ref`) ARE re-surfaced — provides natural operator awareness. Document the trigger explicitly.
- **Low — shared helper extraction touches a working tool.** Mitigation: extract `appendLedgerEvent` + add a `runtime_state_record` regression test in the same step (step 5).
- **Low — `gh` not in `VERIFY_ALLOWLIST`** is irrelevant (the tool does not use `verification-runner.js`; the agent runs `gh` directly via Bash).
- **Low — citation asymmetry (P3 F10):** the body-side `local:meta-state:<id>` is invisible to humans; the registry-side `ledger_ref → dispatch-<id>` is queryable. Document the asymmetry in the issue body footer and the tool's `description`.
