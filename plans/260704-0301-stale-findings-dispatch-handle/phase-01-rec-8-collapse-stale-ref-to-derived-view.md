---
phase: 1
title: "Rec 8 Collapse — stale-ref to derived view"
status: pending
priority: P1
dependencies: []
---

# Phase 1: Rec 8 Collapse — stale-ref to derived view

## Overview
Stop the `stale-ref` producer (sweep), remove `stale-ref` from the category enum at all **four** sites (P0 F1 — fourth site is `schemas/meta-state.schema.json:21`), extend `meta_state_relationships` to surface the same information as a derived view (including the "superseded target" arm), migrate **all 30** existing stale-ref entries (16 `stale` + 14 `reported`) to `superseded` via 30× `meta_state_supersede` (non-atomic, with pre-flight id capture + partial-failure reconciliation), and re-tighten the cap test with 2 headroom.

## Requirements
- Functional: `stale-ref` is no longer a recorded finding kind; it is a query output. Sweep never emits a `stale-ref` follow-up. All 30 stale-ref entries (16 stale + 14 reported) are `superseded` with audit lineage (one change-log `consolidates` all 30). The derived view surfaces findings whose `ref_by`/`reopens` target is stale, missing, **or superseded**.
- Non-functional: registry single-writer; migration runs in the main worktree (serial); the migration is **non-atomic** (30 separate `meta_state_supersede` calls — `consolidated_into` is in `IMMUTABLE_PATCH_FIELDS` so `meta_state_batch` cannot set it; `metaStateBatch` has no `supersede` op); pre-flight captures all 30 ids + a partial-failure reconciliation procedure is documented.

## Architecture
`stale-ref` is a recorded category emitted by `meta_state_sweep` as a follow-up to each newly-stale entry (`meta-state-sweep-tool.js:94-108`, `category:"stale-ref"`, `mechanism_check:false`, `reopens:[t.id]`). The `mechanism_check:false` is intentional (comment `:86-88`) so the follow-up never counts in the cap. The same recursion makes 16 of 28 stale entries self-referential drift. Collapse = stop the producer + remove the category + replace its informational output with a derived query over the relationship graph.

**Migration scope (scout + red-team correction):** the registry has **30** stale-ref entries — 16 `stale` + 14 `reported` (verified against `meta-state.jsonl`). The 14 `reported` have `mc:false` + a 24h `expires_at`; if left, they age to `stale` carrying the removed category, breaking the "clean queue" goal and the stale-count criterion. **All 30** must be migrated. `meta_state_supersede` accepts any finding regardless of status (it only checks `entry_kind === "finding"` + that `consolidated_into` is a real change-log — `meta-state-supersede-tool.js:29-40`), so the 14 `reported` supersede directly.

**Migration mechanism (red-team C1 correction):** `consolidated_into` is in `IMMUTABLE_PATCH_FIELDS` (`core/meta-state.js:280`); `metaStateBatch` enforces the deny-list (`:708-712`, throws `immutable_field`) and has no `supersede` op (`BATCH_OP_TYPES` `:659` = `write|update|delete|archive`). So `meta_state_batch` of `supersede` ops is **impossible**. The real path is **30× `meta_state_supersede`** — non-atomic. `meta_state_supersede` exists precisely to bypass the deny-list (it uses `updateEntry`, not the batch path). Pre-flight captures the ids; partial-failure reconciliation retries the failed ids.

The category enum lives at **four** sites (P0 F1 found the fourth): `core/meta-state.js:63` (exported `META_STATE_FINDING_CATEGORIES`), `:77` (schema `category` z.enum), **`tools/learning-loop-mastra/docs/schemas.md:35`** (doc), and **`schemas/meta-state.schema.json:21`** (JSON Schema enum — parity contract with Zod). `core/loop-introspect.js:4,88-92` imports and spreads the exported const, so removing it there propagates automatically. **INC-6 fix:** all references use the full path for the doc file (not bare `docs/schemas.md`).

## Related Code Files
- Modify: `tools/learning-loop-mastra/__tests__/legacy-mcp/meta-state-sweep-summary.test.js` — **P0 B1 fix (red-team B1 CRITICAL).** The test currently calls `metaStateSweepTool.handler({apply:true})` against the LIVE registry (no `GATE_ROOT` override; sets `OPERATOR_MODE="1"` to bypass the operator gate). This fires on every `pnpm test` run (the pre-commit hook), auto-transitioning past-TTL `reported` entries → `stale` and emitting `stale-ref` follow-up pollution. Fix: switch the test to `tempDir` isolation (template: `meta-state-sweep.test.js:17-46`). The `docs/registry-summary.md` assertion in the same test becomes meaningful against the temp root (where the file WOULD have been written if the bug regressed).
- **INC-4 fix:** Modify `tools/learning-loop-mastra/__tests__/legacy-mcp/fix-loop-design-refs.test.js` — also mutates the live registry on every `pnpm test` run. The test runs `tools/scripts/fix-loop-design-refs.mjs` against `resolveRoot()` with no `GATE_ROOT` override. Same fix: switch to `tempDir` isolation.
- Modify: `tools/learning-loop-mastra/tools/legacy/meta-state-sweep-tool.js` — remove the stale-ref follow-up emission block at `:94-108` (the `metaStateReportTool.handler({category:"stale-ref",...})` call); keep the transition itself. **Also update the stale `description` string at `:40`** (still claims "auto-resolve for entries whose watched file was modified after creation" — a path the handler lacks; red-team L1). **INC-5 fix:** also update the dead-code comment at `:24-25` (references the same auto-resolve path; same staleness; same fix).
- Modify: `tools/learning-loop-mastra/core/meta-state.js` — remove `"stale-ref"` from `META_STATE_FINDING_CATEGORIES` at `:63` AND the schema `category` z.enum at `:77`.
- Modify: `tools/learning-loop-mastra/docs/schemas.md:35` — remove `stale-ref` from the documented category enum row.
- **P0 F1 fix:** Modify `/home/datguy/codingProjects/learning-loop-template/schemas/meta-state.schema.json:21` — remove `"stale-ref"` from the JSON Schema `enum` list. The parity contract (`tools/learning-loop-mastra/mastra/schema-parity.js`) keeps Zod and the JSON Schema in lockstep; missing this site produces a parity drift that `__tests__/schema-fingerprint.test.cjs` will catch at first CI run.
- Modify: `tools/learning-loop-mastra/tools/legacy/meta-state-relationships-tool.js` — **host of the derived view** (red-team/validation P1-W1: `meta_state_query_drift` → `core/query-drift.js` is code-context drift, the wrong tool; the relationships tool already walks `reopens`/`consolidates`/`supersedes` via `buildInverseIndexes`). Add a predicate/output that surfaces findings whose `ref_by`/`reopens` target is stale, missing, **or superseded**.
- Modify: `tools/learning-loop-mastra/__tests__/legacy-mcp/cold-tier-regression.test.js:66-79` — re-tighten threshold 25 → **12** (10 post-migration + 2 headroom for organic drift; red-team M1); update the `:66-72` TODO to reference this plan.
- **P1 F3 fix:** Modify `tools/learning-loop-mastra/__tests__/legacy-mcp/meta-state-sweep-stale-transition.test.js` at THREE sites (the plan originally named only :168):
  - :168 — was pinning a sweep-produced stale-ref follow-up; now asserts no follow-up is produced.
  - **F3 case (L225-269):** `assert.strictEqual(sweepText.stale_reports.length, 3)` retargets to assert `length === 0` (no follow-ups emitted).
  - **F4 case (L271-321):** `assert.strictEqual(followUps.length, 1, "exactly one follow-up report")` retargets to assert `followUps.length === 0`.
- Modify: `tools/learning-loop-mastra/__tests__/legacy-mcp/meta-state-integration.test.js:88`, `meta-state-session-id-roundtrip.test.js:157-160` — used stale-ref as input/filter; retarget to a surviving category.
- **P2 F4 fix:** Modify `tools/learning-loop-mastra/__tests__/phase-e-foundation/fixtures/meta-state-fixtures.js:22` — explicitly set `LEGACY_FINDING_FIXTURE.category = "gate-logic-bug"` (or another surviving category). The snapshot test (`meta-state-relationships-snapshot.test.js:22`) writes the fixtures raw to disk (bypasses Zod validation), so retargeting is cosmetic for THIS test but symbolically required.
- **P2 F4 verification:** add `tools/learning-loop-mastra/__tests__/phase-e-foundation/meta-state-relationships-snapshot.test.js` to the regression-green verification list — confirm the snapshot test still passes after the category retarget.
- Create: a regression test asserting `meta_state_sweep({apply:true})` emits zero stale-ref follow-ups.

## Implementation Steps (TDD — tests first)
0. **(P0 B1 + INC-4) Fix live-registry mutation in two tests** — switch from live `GATE_ROOT` to `mkdtempSync` isolation in:
   - `__tests__/legacy-mcp/meta-state-sweep-summary.test.js` (P0 B1): lines 9, 18 do `const root = resolveRoot()` and `metaStateSweepTool.handler({apply:true})` against the live `meta-state.jsonl`.
   - `__tests__/legacy-mcp/fix-loop-design-refs.test.js` (INC-4): the test runs `tools/scripts/fix-loop-design-refs.mjs` against the live root (`root = resolveRoot()` at line 8; no `GATE_ROOT` override; no `mkdtempSync` isolation); mutates the live `meta-state.jsonl` on every `pnpm test`.

   Template: `meta-state-sweep.test.js:11-46` (`const originalEnv = process.env.GATE_ROOT;` ... `process.env.GATE_ROOT = tempDir;` ... `finally { process.env.GATE_ROOT = originalEnv; }`). After both fixes, neither test mutates the live registry; the assertions become meaningful against the temp root. Run the full test suite **BEFORE** any other step to confirm the live registry is unchanged (read `meta-state.jsonl` before and after — entry count + `updated_at` should match).
1. **Test first (red):** add a regression test that `meta_state_sweep({apply:true})` produces no `stale-ref` follow-up. Run; fails (sweep still emits at `:94-108`).
2. **Stop the producer + fix dead-code comments:** in `meta-state-sweep-tool.js`, remove the `metaStateReportTool.handler({category:"stale-ref",...})` block (`:94-108`) — the operator prompt it carried is replaced by the derived view + Rec 10 surfacing (Phase 3). **Update the stale `description` at `:40`** to match the actual handler (only `checkExpiry` + `checkStaleness`). **INC-5 fix:** also update the dead-code comment at `:24-25` (currently references "file-modification case... is preserved as auto-resolve; not part of this function" — matches nothing in v1; same narrative as L40 description, same staleness). Re-run the regression test → green.
3. **Test first (red):** add a test that `META_STATE_FINDING_CATEGORIES`, the schema enum, AND `schemas/meta-state.schema.json:21` no longer include `stale-ref`, and that `meta_state_report({category:"stale-ref"})` is rejected. Run; fails.
4. **Remove the enum:** delete `"stale-ref"` from `core/meta-state.js:63` (exported const) AND `:77` (schema z.enum) AND `tools/learning-loop-mastra/docs/schemas.md:35` AND `schemas/meta-state.schema.json:21`. Run the test → green. Grep `stale-ref` in `tools/` + `docs/` + `schemas/` → only test/fixture retargets remain.
5. **Test first (red):** add a test that the derived view (`meta_state_relationships` in `meta-state-relationships-tool.js`) surfaces findings whose `ref_by`/`reopens` target is stale, missing, **or superseded**. Run; fails (the "superseded" arm is missing).
6. **Extend the derived view:** in `meta-state-relationships-tool.js`, add the "superseded target" arm so migrating the 30 to `superseded` does not re-trigger the view via inbound refs. Run → green.
7. **Retarget test pins:** update `meta-state-sweep-stale-transition.test.js:168` AND the F3 case (L225-269 `stale_reports.length === 3` → `=== 0`) AND the F4 case (L271-321 `followUps.length === 1` → `=== 0`), `meta-state-integration.test.js:88`, `meta-state-session-id-roundtrip.test.js:157-160`, `phase-e-foundation/fixtures/meta-state-fixtures.js:22` to use a surviving category (e.g. `loop-anti-pattern` or `gate-logic-bug`). Run affected suites → green.
8. **Pre-flight (capture all 30 ids + expires_at distribution):** run `meta_state_list({category:"stale-ref"})` (or a registry grep) and capture all 30 ids — assert the count is 30 (16 stale + 14 reported). **P3 F9 fix:** capture each `expires_at` for the 14 `reported` entries; compute `min(now, earliest expires_at)` and assert it is ≥1 hour from now (hard precondition, not just soft note). If the assertion fails, abort the migration (either re-run sweep to flush to stale first, or delay until the 14 are stale). Record the id list + expires_at distribution in the migration's gate-log. This is the verification step the migration depends on; without it the count is a guess (red-team P1-W7) and a 24h-TTL race could leave entries mid-migration.
9. **Create the change-log:** `meta_state_log_change({change_dimension:"surface", change_target:"meta-state-finding-categories", change_diff:{removed:["stale-ref"]}, reason:"stale-ref category migrated to derived view; recursion collapsed across all 30 entries", consolidates:"<id1>,...,<id30>"})` → captures the change-log id.
10. **Migrate all 30 (serial, in the main worktree):** call `meta_state_supersede({id, consolidated_into:"<change-log-id>"})` 30 times (one per id, in the pre-flight id list). `meta_state_supersede` is operator-gated (`OPERATOR_MODE=1`), accepts any finding status, and uses `updateEntry` (bypasses the `IMMUTABLE_PATCH_FIELDS` deny-list). Run the loop serially (not `Promise.all`) — concurrent calls race on `_expected_version` CAS. This is **non-atomic** — see Rollback/Reconciliation.
11. **Reconcile partial failure:** after the 30 calls, run `meta_state_list({category:"stale-ref"})` again — assert 0 remain. If any failed (e.g. `version_mismatch`), retry those ids. Document the final count in the gate-log.
12. **Re-tighten the cap test:** in `cold-tier-regression.test.js:77`, lower the threshold from 25 to **12** (post-migration count 10 + 2 headroom for organic drift; red-team M1). Update the `:66-72` TODO to reference this plan. Run the cap test → green (count 10 ≤ 12).
13. **Verify:** `pnpm test` green; `grep -rn "stale-ref" tools/ docs/ schemas/` returns no production sites; `meta_state_list({status:"stale"})` total = 12 (the 10 `mc=true|null` real underlying + 2 other `mc=false` stale that survive — verify the actual post-migration count and adjust the criterion if it differs).

## Success Criteria
- [ ] **P0 B1 fix — `meta-state-sweep-summary.test.js` switched to tempDir isolation.** Before any other phase work, the test no longer mutates the live `meta-state.jsonl`. Verified by reading `meta-state.jsonl` before/after a `pnpm test` run (entry count + `updated_at` unchanged).
- [ ] Pre-flight: `meta_state_list({category:"stale-ref"})` returns 30 ids (16 stale + 14 reported); the earliest `expires_at` among the 14 reported is ≥1 hour from now; both recorded in the gate-log.
- [ ] Regression test: `meta_state_sweep({apply:true})` emits zero stale-ref follow-ups (green; fails on pre-fix sweep).
- [ ] `stale-ref` absent from `core/meta-state.js:63`, `:77`, `tools/learning-loop-mastra/docs/schemas.md:35`, AND `schemas/meta-state.schema.json:21` (P0 F1).
- [ ] `meta_state_report({category:"stale-ref"})` rejected by schema.
- [ ] Derived view (`meta_state_relationships`) surfaces stale/missing/superseded ref targets (the "superseded" arm tested).
- [ ] All 30 stale-ref entries migrated to `superseded` under one change-log (30× `meta_state_supersede`, serial not parallel); post-migration `meta_state_list({category:"stale-ref"})` = 0; `meta_state_list({status:"stale"})` total ≈ 12 (verify actual count; the 14 reported no longer age to stale).
- [ ] Cap-test count = 10; threshold re-tightened 25 → 12 (10 + 2 headroom); cap test green.
- [ ] Sweep tool `description` at `:40` updated to match the actual handler.
- [ ] **P1 F3 fix — Three test pins retargeted:** `meta-state-sweep-stale-transition.test.js:168` AND the F3 case at L225-269 (`stale_reports.length === 0`) AND the F4 case at L271-321 (`followUps.length === 0`).
- [ ] **P2 F4 fix — Snapshot fixture retargeted:** `phase-e-foundation/fixtures/meta-state-fixtures.js:22` `LEGACY_FINDING_FIXTURE.category` set to a surviving category (e.g. `gate-logic-bug`); `meta-state-relationships-snapshot.test.js` still passes.
- [ ] `pnpm test` green; `pre-commit` hook (`pnpm test && pnpm fallow:gate`) passes.

## Risk Assessment
- **Critical — `meta-state-sweep-summary.test.js` mutates live registry on every pre-commit (P0 B1):** today's test fires `metaStateSweepTool.handler({apply:true})` against the live `meta-state.jsonl` (no `GATE_ROOT` override; sets `OPERATOR_MODE="1"` to bypass the operator gate). **This is the user's reported confusion mechanism.** Mitigation: Step 0 (before any other phase work) switches the test to tempDir isolation, eliminating live-registry mutation from pre-commit. This must ship before Phase 1's other steps.
- **Critical — sweep emits stale-ref at `:94-108`; removing the enum first breaks sweep at runtime** (schema validation failure on the follow-up report — `writeEntry` throws `InvalidEntryError`, caught by the try-catch at `:111`, logged; graceful degradation, not a crash, but the follow-up is lost). Mitigation: stop the producer (step 2) BEFORE removing the enum (step 4). The regression test (step 1) gates this order.
- **Critical — `meta_state_batch` of `supersede` ops is impossible** (`consolidated_into` in `IMMUTABLE_PATCH_FIELDS` `:280`; `metaStateBatch` enforces the deny-list `:708-712`; no `supersede` op in `BATCH_OP_TYPES` `:659`). Mitigation: use 30× `meta_state_supersede` (uses `updateEntry`, bypasses the deny-list). **Non-atomic** — partial-failure reconciliation in step 11.
- **Critical — 30 stale-ref entries, not 16** (14 `reported` with a 24h TTL age to stale carrying the removed category). Mitigation: migrate all 30 (step 8 pre-flight asserts the count; step 10 migrates the full set).
- **High — `stale-ref` lives at FOUR sites, not three:** `core/meta-state.js:63` + `:77` + `tools/learning-loop-mastra/docs/schemas.md:35` + **`schemas/meta-state.schema.json:21`** (P0 F1). Remove from all four or the parity contract (`mastra/schema-parity.js`) reports drift and `schema-fingerprint.test.cjs` fails at first CI run.
- **High — test pins break at THREE sites, not one:** `meta-state-sweep-stale-transition.test.js` has pins at :168 + F3 case (L225-269 `stale_reports.length === 3`) + F4 case (L271-321 `followUps.length === 1`). Step 7 retargets all three.
- **Medium — cap-test post-migration count of 10 has zero headroom against the original threshold of 3** (any new stale `mc=true|null` finding pushes the count above 3). Mitigation: re-tighten the threshold to 12 (10 + 2 headroom for organic drift); exceeding 12 requires a threshold bump or finding resolution (INC-8 wording fix).
- **Medium — 24h TTL race window during migration (F9):** the 14 `reported` entries have `expires_at` within 24h; if any age to `stale` mid-migration, the plan still succeeds (status-agnostic) but the count drift may break the pre-flight assertion. Mitigation: step 8 captures `expires_at` distribution and asserts the earliest is ≥1h from now (hard precondition).
- **Low — cap-test re-tighten to 3 (the original) fails** because 10 real underlying issues remain. This plan re-tightens to 12; re-tightening to 3 is a follow-up (resolving the 10 real issues), not this plan.
- **Low — fixture `LEGACY_FINDING_FIXTURE.category: "stale-ref"` (F4) is consumed by snapshot test via raw JSON write** (bypasses Zod validation; cosmetic-only today). Mitigation: step 7 retargets explicitly; documented for future schema-on-read validation.

## Rollback / Reconciliation
- **Pre-flight (step 8):** run against a throwaway worktree/registry copy first — `meta_state_batch` has no `apply`/`dry` flag (validation P1-W7), so the migration is not previewable. Copy `meta-state.jsonl` + `runtime-state.jsonl` to a temp location, point the MCP server at it, run steps 9-11 there, then run against the live registry.
- **Partial failure (step 11):** if some `meta_state_supersede` calls fail, the change-log's `consolidates` list (all 30) disagrees with reality. Reconciliation: `meta_state_list({category:"stale-ref"})` returns the un-migrated ids; retry those. A superseded entry's `consolidated_into` is harmless on a non-superseded entry, so a failed call leaves no damage — only the absence of the transition.
- **Full rollback (if the whole phase needs undoing):** the change-log is immutable (cannot delete); to "undo," patch each superseded finding back to its prior status (`meta_state_patch({id, status:"stale"})` for the 16, `status:"reported"` for the 14 — `status` is patchable, not in `IMMUTABLE_PATCH_FIELDS`) and restore the enum at all three sites. The change-log stays as an audit record of the attempt.
- **24h TTL window:** run steps 8-11 within the same session if possible; if the 14 `reported` entries age to `stale` mid-migration, they are still `category:"stale-ref"` and migrate the same way (the migration is status-agnostic).
