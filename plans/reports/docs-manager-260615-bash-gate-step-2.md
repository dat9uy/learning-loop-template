# Docs Manager Report — Step 2 (bash-gate debate infra)

**Date:** 2026-06-15
**Task:** Assess whether Step 2 implementation requires updates to `./docs` and apply any needed updates.
**Step 2 shipped:**
- New MCP tools: `gate_override`, `gate_check_recurrence` (registered in `tools/manifest.json` and `agent-manifest.json` gate group).
- New files: `core/gate-override.js`, `core/gate-decision-log.js`, `core/recurrence-tracker.js`, `tools/gate-override-tool.js`, `tools/gate-check-recurrence-tool.js`, `hooks/recurrence-check-on-start.js`, plus `.cjs` wrappers.
- Modified: `hooks/bash-gate.js` (decision visibility via hookSpecificOutput), `core/gate-logic.js` (override skip), `hooks/lib/protocol-adapter.js` (formatHookDecision), `.claude/settings.json` and `.factory/settings.json` (SessionStart hook).

---

## Files Updated

### 1. `docs/system-architecture.md` (4 edits)

| Section | Change | Rationale |
|---|---|---|
| Bash Coordination Gate Flow | Added steps 8-10: promoted rules check + override skip + decision log append | Step 2 added `loadPromotedRules`, `applyPromotedRules`, and `appendDecisionLog` to the bash gate flow |
| Constraint Gate MCP Server | Tool count: 31 → 33; added `gate_override` and `gate_check_recurrence` to the list | Two new MCP tools registered in `manifest.json` |
| check_gate subsection | Added `gate_override` and `gate_check_recurrence` subsections with descriptions | New tools need operator-facing documentation |
| Known Issues | Added F13: Recurring False-Positive Escalations — RESOLVED | Step 2's primary feature is recurrence detection; documenting as resolved finding |

### 2. `docs/operator-guide.md` (3 edits)

| Section | Change | Rationale |
|---|---|---|
| MCP Tools | Tool count: 31 → 33; added `gate_override` and `gate_check_recurrence` to key enforcement tools | Operator needs to know these tools exist |
| Write Domain Rules | Added "Gate override" subsection explaining `gate_override` usage, TTL, audit trail | Operator needs to know how to use the override tool |
| New section: Recurrence Detection | Added section explaining `gate_check_recurrence`, auto-filing, SessionStart hook, thresholds | Step 2's primary feature needs operator-facing docs |

---

## Files NOT Updated (and why)

| File | Reason |
|---|---|
| `docs/record-system-architecture.md` | Step 2 does not touch the record ledger, index extraction, or state machine. No material changes. |
| `docs/registry-summary.md` | Auto-generated by `meta_state_sweep`. Will refresh on next sweep. |
| `docs/trajectory.md` | Step 2 is infrastructure (gate tooling), not a bridge or meta-surface milestone. No trajectory impact. |
| `docs/charter.md` | No charter-level changes. Step 2 is operational enhancement, not scope change. |
| `docs/philosophy.md` | No philosophical changes. Override and recurrence are operational mechanisms, not design principles. |
| `docs/meta-state-lifecycle.md` | No new entry kinds or status transitions. Recurrence findings use existing `finding` kind. |
| `docs/observation-vs-meta-state.md` | No layer separation changes. |
| `docs/mcp-server-restart-protocol.md` | No restart protocol changes. |
| `docs/artifact-concepts.md` | No artifact concept changes. |
| `docs/problem-classification.md` | No new problem classes. |
| `docs/red-team-review.md` | No security posture changes. |
| `docs/vendor-vnstock-installer.md` | Unrelated. |
| `docs/operator-guide-vnstock-appendix.md` | Unrelated. |

---

## Validation

Ran `validate-docs.cjs` on `./docs/`. All pre-existing warnings are unchanged (hook event names, function names in prose, env vars not in `.env.example`). No new warnings introduced by these edits.

---

## Unresolved Questions

None.

**Status:** DONE
**Summary:** Updated `system-architecture.md` and `operator-guide.md` with minimal, accurate additions for the two new MCP tools (`gate_override`, `gate_check_recurrence`), the decision log flow, and the recurrence detection feature. No other docs required updates.
**Concerns/Blockers:** None
