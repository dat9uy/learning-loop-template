---
phase: 3
title: "Resolve Findings + Optional Union"
status: pending
priority: P3
effort: "low"
dependencies: [1, 2]
---

# Phase 3: Resolve Findings + Optional Union

## Overview

Close the loop: resolve `meta-260715T0349Z` (fixed via Phases 1–2), record a change-log for the `meta-260709T1017Z` reframe (the finding's description was already patched to v2 on 2026-07-15 to surface the two-target mismatch and is kept OPEN — verify it landed, do NOT resolve it), PATCH (NOT append) PROCESS_HINTS row 8 so the pretest-seed + commit/regen split convention is discoverable by future sessions, and ship all three refinements surfaced by Phases 1–2 + red-team review (per Validation Session 1, 2026-07-15): C1 — `meta_state_refresh_file_index` `status: "no-op"` return; C2 — local `test:cold-session` seed prefix; C3 — `.gitattributes` `runtime-state.jsonl merge=union` for defense in depth.

## Requirements

- **Functional:** `meta-260715T0349Z` resolved (fixed via Phases 1–2) AFTER preflight (`meta_state_query_drift` clean + `meta_state_refresh_file_index` for edited paths); `meta-260709T1017Z` reframed (description patched to v2 on 2026-07-15 to name the two-target mismatch) and **kept OPEN** — a change-log records the architectural-tension decision; the finding is NOT resolved. PROCESS_HINTS row 8 is PATCHED (not appended) at `loop-introspect.js:137` + mirrored at `.factory/hooks/loop-surface-inject.cjs:44` + `loop-get-instruction-tool.js:64` `HINT_SUGGESTIONS_PROCESS[8]` (3 storage sites total — not 4); length-assertion test stays at `=== 9`.
- **Non-functional:** registry writes go through MCP tools (`meta_state_resolve`, `meta_state_log_change`); no direct file writes. The PROCESS_HINTS mirror must update all parity sites (see Risk 2).

## Architecture

Three groups of work, each independently committable:

**A. Resolve + reframe findings (required).**
- **Preflight (Finding 4 from `## Red Team Review`):** before calling `meta_state_resolve`, run `meta_state_query_drift` and confirm zero open `mechanism_check: true` findings have an orphan-evidence state. The consult-gate at `gate-logic.js:661-721` (enforced by `meta-state-resolve-tool.js:84-117`) iterates every open mechanism-checked finding and re-hashes each `evidence_code_ref`. If any orphan exists, the gate returns `resolution_evidence_required` and blocks the resolve. The target finding `meta-260715T0349Z` has `evidence_code_ref:"tools/learning-loop-mastra/core/meta-state.js:679"` (`meta-state.jsonl:308`) — Phase 1 edits that function, so the fingerprint must be re-recorded via `meta_state_refresh_file_index` first.
- `meta_state_resolve({ id: "meta-260715T0349Z-...", resolution: "Fixed: true no-op in upsertFileIndexEntry (Phase 1) + gitignore file-index.jsonl + CI seed-before-cold-session (Phase 2). Re-seed is git-idempotent; cold-tier cache stays warm on no-change re-seeds after first cold build.", resolved_by: "operator" })`.
- `meta-260709T1017Z` is **already reframed** (description patched to v2 on 2026-07-15 to surface the two-target mismatch; kept OPEN). Do NOT resolve it. Verify the reframe landed (`meta_state_list({id})` shows version 2 + the `[reframe 2026-07-15] TWO-TARGET MISMATCH` block + status `open`), then record the architectural-tension decision as a change-log via `meta_state_log_change` (semantic, target `meta-state.jsonl`, reason ≥20 chars) so the "keep in-PR commits + manual `git merge-file --union`" compromise is auditable. The real fix (split the change-log stream into a separate true-append-only file) is deferred.
- If `meta_state_resolve` returns `{ resolved: false, reason: 'already_terminal', current_status }` (no exception — `meta-state-resolve-tool.js:67-82`), check status with `meta_state_list` first and branch accordingly (Red-team #13 from `260714-2012`).

**B. PROCESS_HINTS patch (NOT a new row).** PR #58 already shipped the row covering pretest seed + `SKIP_PRESEED` escape + per-path refresh (at `core/loop-introspect.js:137` for `PROCESS_HINTS` index 8, mirrored byte-for-byte at `.factory/hooks/loop-surface-inject.cjs:44`). Adding a 10th row breaks `gate-logic-consult-checklist-fallow-brief.test.js:74` (`processHints.length === 9`) and the cold-session parity test. **Do NOT append a new row.** Instead, **patch row 8** with the one new fact: `file-index.jsonl` is now untracked (regen artifact). Also patch the `loop-get-instruction-tool.js:64` `HINT_SUGGESTIONS_PROCESS[8]` placeholder to mention "untracked regen artifact". The dynamic bounds check in `cold-session-discoverability.test.cjs:401-415` (Finding 12 from `## Red Team Review`) is automatically satisfied for an index-8 patch — verify with `pnpm test:cold-session` after the edit. Audit distinction (Red-team #11 lesson from `260714-2012`): `seed-file-index.mjs` writes no gate-log; `meta_state_refresh_file_index` is audited — state this in the patched row text.

**C. Required refinements (Validation Session 1, 2026-07-15 — all three shipped).**
- **C1 — `meta_state_refresh_file_index` no-op return (SHIPPED).** After Phase 1, the tool returns `status: "refreshed"` / `cache_hit: false` even on an unchanged path (misleading but not incorrect). Refine: detect the no-op (compare stored hash to computed hash before upsert) and return `status: "no-op"`. **Do NOT set `cache_hit: true`** — the tool's documented contract is `cache_hit: false` always (`meta-state-refresh-file-index-tool.js:34`); changing that field is a contract change that requires auditing all `cache_hit` consumers AND updating the tool's description. The `status: "no-op"` discriminator alone is sufficient signal without touching the contract. Check for any consumer asserting `status === "refreshed"` first.
- **C2 — local `test:cold-session` seed prefix (SHIPPED).** Phase 2 Risk 2: `test:cold-session` has no seed prefix. Prepend `tools/learning-loop-mastra/tools/handlers/scripts/seed-file-index.mjs && ` to `test:cold-session` in `package.json` (mirrors `test`). Symmetry with the `test` script. Verification: read `cold-session-discoverability.test.cjs` first to confirm it does not assert gap-close behavior against an empty index; if it does, switch to document-only.
- **C3 — `runtime-state.jsonl merge=union` (SHIPPED, defense in depth).** Create `.gitattributes` with `runtime-state.jsonl merge=union` and an inline comment linking finding `meta-260709T1017Z`. True append-only → safe. Costs nothing; cheap to revert if a conflict pattern actually emerges.

## Related Code Files

- **Modify (B):** `tools/learning-loop-mastra/core/loop-introspect.js` (PATCH row 8 of canonical `PROCESS_HINTS` at line 137), `.factory/hooks/loop-surface-inject.cjs` (PATCH row 8 of `LOCAL_PROCESS_HINTS` at line 44, byte-identical mirror), `tools/learning-loop-mastra/tools/handlers/loop-get-instruction-tool.js` (PATCH `HINT_SUGGESTIONS_PROCESS[8]` at line 64 — note: `HINT_KEY_MAP_PROCESS` at line 24 and `HINT_SUGGESTIONS_PROCESS` at line 50 live in this file, NOT in `core/loop-introspect.js`). **No length-assertion bump** (`gate-logic-consult-checklist-fallow-brief.test.js:74` `length === 9` stays — we are patching, not appending). The dynamic bounds check at `cold-session-discoverability.test.cjs:401-415` is automatically satisfied.
- **Modify (C1, REQUIRED per Validation Session 1):** `tools/learning-loop-mastra/tools/handlers/meta-state-refresh-file-index-tool.js` (no-op detection + `status: "no-op"` branch; `cache_hit` stays `false`)
- **Modify (C2, REQUIRED per Validation Session 1):** `package.json` `test:cold-session` script (prepend `tools/learning-loop-mastra/tools/handlers/scripts/seed-file-index.mjs && ` to mirror `test`)
- **Create (C3, REQUIRED per Validation Session 1):** `.gitattributes` with `runtime-state.jsonl merge=union` line + comment linking finding `meta-260709T1017Z`
- **Modify (single-PR packaging, Validation Session 1):** all three phases ship in ONE PR (per Session 1 decision). Order commits: Phase 1 (no-op test + code + JSDoc) → Phase 2 (gitignore + `git rm --cached` single commit + CI step) → Phase 3 (PATCH row 8, registry resolve, change-logs, .gitattributes, C1, C2).
- **MCP:** `meta_state_resolve` (×2), `meta_state_log_change` (record the commit/regen split as a change-log)

## Implementation Steps

1. Run the full suite green: `pnpm test` (confirms Phase 1 + Phase 2 landed cleanly).
2. **(B)** Read `core/loop-introspect.js` `PROCESS_HINTS[8]` (line 137) current text. **PATCH** it (do NOT append) with the one new fact: `file-index.jsonl` is untracked (regen artifact). Mirror the patch to `.factory/hooks/loop-surface-inject.cjs:44` (byte-identical mirror). Patch `loop-get-instruction-tool.js:64` `HINT_SUGGESTIONS_PROCESS[8]` to mention "untracked regen artifact". The existing `HINT_KEY_MAP_PROCESS["file-edit-drift-and-fingerprints"]: 8` mapping (`loop-get-instruction-tool.js:28`) and the dynamic bounds check (`cold-session-discoverability.test.cjs:401-415`) remain valid — no length change. Run `pnpm check:freshness` / `pnpm test:cold-session` to confirm the patch surfaces and tests still pass.
3. **(A)** **Preflight:** run `meta_state_query_drift`; confirm zero open `mechanism_check: true` findings have orphan evidence. Re-record fingerprints for any edited paths via `meta_state_refresh_file_index` (Phase 1 edits `core/meta-state.js` — that path needs a refresh). Then resolve `meta-260715T0349Z` via `meta_state_resolve` (fixed). Verify `meta-260709T1017Z` is version 2 + OPEN + carries the `[reframe 2026-07-15] TWO-TARGET MISMATCH` block (already patched this session) — do NOT resolve it. Record two change-logs via `meta_state_log_change`: (i) the file-index commit/regen split + no-op (target `file-index.jsonl` + `core/meta-state.js#upsertFileIndexEntry`); (ii) the `meta-260709T1017Z` reframe / two-target-mismatch architectural-tension decision (target `meta-state.jsonl`). reason ≥20 chars each.
4. **(C1, REQUIRED)** Grep for `status === "refreshed"` / `"refreshed"` consumers; if none depend on the always-"refreshed" wording, add the no-op branch to `meta-state-refresh-file-index-tool.js` (compare stored vs computed hash before upsert; return `status: "no-op"` on match; `cache_hit` stays `false` per contract). Add/adjust a test in the sibling test file for the no-op path.
5. **(C2, REQUIRED)** Read `cold-session-discoverability.test.cjs` to confirm it does NOT assert against an empty/missing index. If safe, edit `package.json` `test:cold-session` to prepend `tools/learning-loop-mastra/tools/handlers/scripts/seed-file-index.mjs && ` (mirror `test`). If it DOES assert against absence, leave `test:cold-session` as-is and add the local-run prerequisite note to PROCESS_HINTS row 8 instead — do NOT silently change the test's semantics.
6. **(C3, REQUIRED)** Create `.gitattributes` with `runtime-state.jsonl merge=union` and an inline comment linking `meta-260709T1017Z`. Tiny diff, defense in depth.
7. Commit per conventional format (no AI refs). The 3 phases land in ONE PR (Validation Session 1) — suggested commit order: `feat(core): true no-op in upsertFileIndexEntry` (Phase 1) → `chore(repo): gitignore + regen-only file-index.jsonl + CI seed step` (Phase 2) → `chore(registry): resolve + reframe + PROCESS_HINTS row 8 patch + C1/C2/C3` (Phase 3). Refresh the file-index if any code edit in this phase changed a cited path (now a no-op-friendly operation).

## Success Criteria

- [ ] Preflight `meta_state_query_drift` passes (zero orphan-evidence findings); `meta_state_refresh_file_index` re-recorded for `core/meta-state.js`.
- [ ] `meta-260715T0349Z` resolved (fixed) in the registry; `meta-260709T1017Z` verified version 2 + OPEN + reframed (NOT resolved).
- [ ] Two change-log entries: (i) commit/regen split + no-op; (ii) `meta-260709T1017Z` two-target-mismatch reframe / architectural-tension decision.
- [ ] PROCESS_HINTS row 8 PATCHED (not appended) at `loop-introspect.js:137`, mirrored at `loop-surface-inject.cjs:44`, `HINT_SUGGESTIONS_PROCESS[8]` patched at `loop-get-instruction-tool.js:64`; length-assertion test stays at `=== 9`; dynamic bounds check at `cold-session-discoverability.test.cjs:401-415` passes.
- [ ] (C1) `meta_state_refresh_file_index` returns `status: "no-op"` on an unchanged path with `cache_hit: false`; consumers checked; tool description NOT changed.
- [ ] (C2) `package.json` `test:cold-session` has the seed prefix; cold-session-discoverability test does NOT regress against a seeded index.
- [ ] (C3) `.gitattributes` has `runtime-state.jsonl merge=union` + inline comment linking `meta-260709T1017Z`.
- [ ] `pnpm test` + `pnpm test:cold-session` green end-to-end.
- [ ] All three phases ship in ONE PR (commit order: Phase 1 → Phase 2 → Phase 3).

## Risk Assessment

- **RISK 1 — PROCESS_HINTS parity drift (PATCH not append).** Row 8 of `PROCESS_HINTS` (`loop-introspect.js:137`) is mirrored byte-for-byte at `loop-surface-inject.cjs:44`. A patch must update **both** in the same commit, or the cold-session parity test fails. The `length === 9` assertion at `gate-logic-consult-checklist-fallow-brief.test.js:74` stays — we are patching an existing row, not appending a 10th. Re-verify the mirror is still byte-identical for the unedited portion after patching.
- **RISK 2 — `meta_state_resolve` consult-gate block.** Resolving `meta-260715T0349Z` triggers the consult-gate (`gate-logic.js:661-721`), which iterates every open `mechanism_check: true` finding and re-hashes `evidence_code_ref`. If any orphan exists (file-index stale, edited path not re-recorded), the gate returns `resolution_evidence_required` and blocks resolve. **Mitigation:** preflight with `meta_state_query_drift` and `meta_state_refresh_file_index` for any edited paths BEFORE calling resolve. The resolve itself is also a structured non-error response (`{ resolved: false, reason: 'already_terminal', current_status }`) for terminal entries — check status with `meta_state_list` first.
- **RISK 3 — C2 fixture conflict (now required).** The cold-session discoverability probe may assert gap-close behavior *against an empty/missing index*. Prepending the seed could mask that fixture and change the test's meaning. **Mitigation:** Implementation Step 5 mandates reading the test first; if it asserts against absence, fall back to document-only in PROCESS_HINTS row 8 and revert the package.json edit. Do NOT silently change the test's semantics.
- **RISK 4 — single-PR complexity (now required).** Validation Session 1 chose 1 PR for all 3 phases. The PR surface is ~30 lines code + 1 workflow step + 1 file-entry gitignore + 1 metadata-fingerprint refresh + 1 registry resolve + 2 change-logs + 1 PROCESS_HINTS patch + 1 .gitattributes. Mixed concerns in one review. **Mitigation:** commit order is fixed (Phase 1 → Phase 2 → Phase 3) so reviewers can read sequentially; PR description explains the commit order and references this plan.
- **RISK 5 — C1 contract change (resolved).** `cache_hit: false` is the documented contract (`meta-state-refresh-file-index-tool.js:34`). C1 now only changes `status` (`refreshed` → `no-op`); `cache_hit` stays `false`. No consumer audit needed.
- **RISK 6 — C3 misalignment (resolved).** `runtime-state.jsonl merge=union` ships (Validation Session 1). Tiny `.gitattributes` entry; comment links `meta-260709T1017Z`. No measurable cost; revert-by-delete if a future finding contradicts the choice.