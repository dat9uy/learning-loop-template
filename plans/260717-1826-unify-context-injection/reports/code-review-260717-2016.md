# Code review — unify-context-injection (5 commits 964e14c..092011c)

**Status:** DONE_WITH_CONCERNS • **Reviewers:** main session (Stage-1 spec compliance) + code-reviewer subagent (Stage-2 quality, independent) • **Verdict:** NOT merge-ready — 2 Critical findings

## Verification gate (fresh evidence)

- `pnpm test:iter`: **2218 tests / 448 suites green** (this session, bail=1).
- `check_runtime_agnostic` on `core/hint-renderer.js`: 6/6 pass (Phase-2 step-9 audit, not recorded in journal — ran post-hoc).
- Backfill byte-identity: all 8 rule `hint_text` values **byte-match** the pre-Phase-3 `PROCESS_HINTS` rows (external check: `git show HEAD~5:loop-introspect.js` vs live `meta-state.jsonl`).
- CLI smoke: `hint-render.mjs --channel claude-session-start --provenance` exit 0, 25ms.
- Partition sizes vs 10k cap: disc 5009, process 7849 chars (headroom OK).

## Critical

### C1. Test-pollution findings committed to live `meta-state.jsonl`
4 junk `hint-downgrade` findings with test session ids (`test-summary-001` ×2, `test-session-abc`, `test-summary-tier-x`), `status: open`, incl. **duplicate ids** (`meta-260717T1915Z-hint-downgrade` ×2, `meta-260717T1917Z-hint-downgrade` ×2 — same-minute `generateId` collisions across test runs). Introduced by commit `e04c737` (TDD cycles wrote to the live registry; later test versions use temp dirs). Effects: active-finding counts inflated, `loop_describe`/sweep/drift queries polluted, duplicate ids break the versioned-append projection (arbitrary max_by winner). Also **undisclosed** in the completion report's PR-body registry deltas — violates `rule-pr-body-registry-deltas` item (c).
**Fix:** sweep the 4 lines from `meta-state.jsonl`; disclose the removal in the PR body.

### C2. `loop_get_instruction` returns wrong hints when any rule-derived entry is skipped
`buildProcessHints()` (`core/loop-introspect.js:187-194`) silently **drops** skipped rule-derived entries (missing/inactive rule → `text: ""` fallback is falsy → not pushed), shrinking + shifting the array. `resolveHint` (`tools/handlers/loop-get-instruction-tool.js:48-57, 61-77`) indexes that shrinkable array with **fixed registry-order maps** built at module load. Result: slug and numeric lookups after a skipped position return the *next* hint's text paired with the *correct* suggestion (mismatched, silent). Triggers: rule retirement (routine lifecycle), `scope_predicate` filtering (3 of 8 rules carry `project_has_learning_loop_mcp`; dropped in roots without `.mcp.json`), cwd≠root (S4 below). Subagent empirical proof: `fallow-gate-triage` → returned "Assertinvariant at boundary…" text with "Fallow gate triage…" suggestion. Violates `hint-registry.js:20-21` ("registry order = numeric-index back-compat"). Pre-change code was immune (frozen 10-entry const). No test covers it (only behavioral lookup test uses shift-immune index 0).
**Fix:** make `buildProcessHints` stable-length (placeholder on skip — cheapest, preserves the registry-order contract) OR de-positionalize slug resolution (resolve text directly from registry entry / `rule.hint_text`). Add a test with a deactivated rule.

## Important

### I1. Renderer not wired to production; docs claim it is (spec deviation, silent)
Phase-02 required: `.claude` hooks + factory hook "become renderer clients". Not done — no hook imports `core/hint-renderer.js`; only the CLI + tests consume it. Yet the new `docs/architecture.md` section says push surfaces are "rendered by `core/hint-renderer.js`", and "Four surfaces, one renderer". Also renderer output format (`— ` prefix) ≠ production hook format (numbered list, `session-start-inject-process-hints.cjs:33`). Byte-identity across surfaces is in fact achieved via the shared registry+builders — so either downgrade the renderer to "inspection-only" in docs, or wire the hooks. Current state = 216 LOC of production-dead pipeline + docs describing an unbuilt architecture.
**Fix:** pick one: wire hooks (phase-02 as specced) or correct docs + reposition renderer as debug tooling (YAGNI audit on the channel table).

### I2. CLI output is not byte-exact for 8/10 process hints (Goal 4 failed)
`hint-render.mjs:65-69` mocks `rulesById` with `[mocked hint_text for <slug>]` (verified live: partition 1 prints the mock strings). Real render = 7849 chars; mocked ≈ 2600 shorter → the CLI cannot verify budget compliance, its primary purpose. Phase-04 spec: "Prints the byte-exact render the runtime would inject."
**Fix:** load real rules via `loadPromotedRules(process.cwd())` (with a `--root` escape), keep mock behind a flag.

### I3. Dead "oracle" consts + circular byte-identity test
`loop-introspect.js:120` `DISCOVERABILITY_HINTS` (unexported, referenced nowhere) and `:125` `PROCESS_HINTS = Object.freeze([...])` (~9KB duplicated prose) retained with comments claiming they back `rule-derived-process-hints.test.cjs`. That test **never references the const** — it compares renderer-sidecar output against `buildProcessHints()`, both projections of the same live registry (circular: passes even if a rule's hint_text is edited). Phase-02 success criterion ("zero `PROCESS_HINTS = Object.freeze` outside hint-registry.js") failed; test comment even says "Phase 4 removes it" — Phase 4 didn't. Suite header comment (loop-introspect.js:15-17) says the consts "were deleted" — false for PROCESS_HINTS.
**Fix:** delete both consts; either make the byte-identity test assert against a frozen expected array in the test file, or rename it to "renderer ≡ builder consistency" and drop the byte-identity claim.

### I4. `buildProcessHints()` resolves rules from `process.cwd()` — inconsistent root handling
`core/loop-introspect.js:175`. Both production callers have correct roots in scope but call bare: factory hook loads `gateLogic.loadPromotedRules(projectRoot)` for the count (line 136) yet lets hint text load from cwd; `loop-describe-tool.js` has `promotedRules` from `resolveRoot()` at the call site. Under cwd≠root: 8/10 process hints silently vanish (and via C2, wrong-content lookups). Divergent skip semantics: renderer warns, builder silently skips. Was previously a pure const — new I/O + fragility introduced without need.
**Fix:** pass `rulesById` from the already-loaded rules at both call sites.

### I5. Promote-tool `hint_text` gate fires before the preview branch (regression)
`meta-state-promote-rule-tool.js:93-110` runs before `if (preview)` — `preview: true` + agent-checklist + no hint_text now rejected, contradicting "Use preview:true to test pattern matches without activating". Also zod `.min(20)` on the schema rejects 1–19-char hint_text before the handler's actionable message (generic error, no gate-log entry).
**Fix:** move the gate after the preview branch (or exempt `preview`).

### I6. `greedyPartition` emits over-budget partitions; docstring false
`hint-renderer.js:82-87`: a single entry larger than `charBudget` is forced through as an over-budget partition (subagent confirmed: budget 5000 → 8003-char partition), no warning; header claims "every partition fits under the requested charBudget". Latent today (largest hint ≈1.9k), but `hint_text` has zod `min(20)` and **no max** (`meta-state.js:480`) — one verbose promoted rule silently breaches the 10k cap once hooks are wired (I1 makes this latent).
**Fix:** oversized entry → own partition + provenance warning; correct the docstring; consider a `hint_text` max or render-time byte audit (`Buffer.byteLength`, cf. char-vs-byte undercount ≈30–60B today — Minor).

### I7. CLI `--partition` NaN → unhandled TypeError, exit 1 (contract: exit 2)
`hint-render.mjs:33,78-84`: `parseInt(undefined|"abc")` → NaN; range check `NaN < 0 || NaN >= len` is false → `partitions[NaN]` = undefined → `process.stdout.write(undefined)` throws. Both `--partition` (no value) and `--partition abc` confirmed.
**Fix:** `Number.isInteger` guard → exit 2 with message.

### I8. Phantom/tautological renderer tests
`hint-renderer.test.cjs:64-68` (factory channel): loop variable `e` unused — asserts `text.length > 500` five identical times; comment claims "each of the 26 registry slugs" but no slug checked. `:28-44` (claude channel) runs **without** `rulesById` → passes with 8/10 process hints absent; no test renders the claude channel with rules (partition-count flip at ~9k chars unasserted). `hint-render-cli.test.cjs:3-4` header claims byte-equality vs in-process renderer — no such assertion exists (smoke-level only).
**Fix:** assert real content (slugs/provenance) with a real `rulesById`; fix or delete the loop; correct the CLI test header.

## Minor

- **M1.** Stale comments: factory-hook header (`:13-14`) — "edit core/loop-introspect.js … until Phase 2" (Phase 2 shipped; source is now registry + `rule.hint_text`).
- **M2.** Dead `entries` param in all 4 channel renderers (`hint-renderer.js:99,122,134,164`) — filtered registries silently ignored; unknown-channel return (`:203-205`) lacks the `warnings` key (shape inconsistent).
- **M3.** Char-vs-byte budget undercount (`.length` vs UTF-8 bytes; ~20 em dashes + `≤`/`§` in hint text) — bounded today (~60B of 500+ headroom), never audited.
- **M4.** Injection inherits gate `scope_predicate` filtering (`buildProcessHints` reuses `loadPromotedRules`, `gate-logic.js:784-796`) — 3 hints vanish in roots without `.mcp.json`; renderer warning misattributes cause ("rule missing or has no hint_text"). Undocumented coupling.
- **M5.** Behavior change (intended, flag): previously-unmapped process slugs (6 of 10) now resolve in `loop_get_instruction` — wire-visible for previously-erroring keys.
- **M6 (process).** Phase-3 backfill ran via direct `core.updateEntry` from `/tmp` after an MCP `meta_state_patch` "Invalid input" — never root-caused ("likely session-mode gate" is a guess; the same "Invalid input" appears in-test this session, consistent with a stale long-running MCP server schema). Data verified correct post-hoc (byte-identity check above).
- **M7 (process).** Phase-2 step-9 `check_runtime_agnostic` audit not recorded in journal (passes post-hoc, 6/6).

## Verified OK

- Mirror + probe deletion complete (grep-verified: no `LOCAL_*`, `spawnAndCall`, `reportMcpConnectionFailure`, MCP SDK import in factory hook).
- H6 nag removal mitigated: `consult-checklist-process-hints-coverage.test.js` now enforces both directions at CI (rule→registry row; no orphan derived entries).
- `readToolCount` JSONC regex strips only full-line unquoted `//` lines; mirrors `loop-introspect.js:51`; cannot mangle strings.
- `reportHintDowngrade` dedupe logic unchanged; current tests hermetic (temp-dir registries).
- All 8 rules carry `hint_text` (372–1098 chars) on max-version rows; numeric back-compat 0–25 verified from repo root (26 → "Unknown hint key").
- Test relaxations (frozen assertion, count 9→10) documented inline with reasons.
- `placement.yaml` registers both new core files; placement-manifest + runtime-agnostic suites green.
- `resolveHint` per-key `loadPromotedRules` is mtime+size cached — no N+1 reads.

## Recommended fix order

1. **C1** — sweep the 4 junk findings; disclose in PR body per `rule-pr-body-registry-deltas`.
2. **C2** — stable-length `buildProcessHints` (or non-positional slug resolution) + deactivated-rule test.
3. **I1/I2/I3** — decide renderer fate (wire hooks vs debug-only docs); CLI real rules; delete dead consts; make byte-identity test real or rename.
4. **I4** — pass `rulesById` at both call sites.
5. **I5–I8** — preview-gate order; partition oversize + NaN handling; test de-phantoming.
6. Minor comment/doc cleanups (M1, docs section accuracy).

## Unresolved questions

1. C2 fix shape: stable-length projection (preserves numeric back-compat cheaply) vs de-positionalizing `loop_get_instruction` — recommendation: stable-length placeholder.
2. Is `scope_predicate` filtering of *injection* intended (M4)? If yes, document + fix the warning text; if no, unfiltered rule read for hints.
