---
record_type: evidence
capability: vnstock-data
dimension: runtime
scope: sandbox
validation_status: passed
claim_support: supports
output_level: runtime-captured
created: "2026-05-11T14:35:00+07:00"
substrate: product-api-venv-python-3-12
---

# Runtime 403 Fix - vnstock_data - 20260511

## Summary

The wrapper config path now points at `.vnstock` root, so the vendor installer and runtime agree on where `user.json`, `api_key.json`, and `device.id` live.

The API source patch lives in `product/api/src/vendor_compat/`. It injects a generated `Device-Id` header and matching `device_id` cookie for VCI requests when the installed `vnstock_data` package does not do so itself.

## Verification

- `bash scripts/install-vnstock.sh` from `product/api` exited 0 and skipped a reinstall because `vnstock_data` already imports.
- `.vnstock/user.json` is a file, not a directory.
- Missing top-level `device.id` was migrated from the prior backup directory with mode `0600`.
- Header check confirmed `get_headers(data_source="VCI", random_agent=False)` contains `Device-Id` and `Cookie`.
- Opt-in smoke test passed: `VNSTOCK_SMOKE_TEST_ALLOW_LIVE=1 product/api/.venv/bin/python -m pytest product/api/tests/test_vci_smoke.py -m network -v --tb=short`.
- Live route handler check passed after NaN sanitization: `list_equity()` returned `row_count 1742` and columns `symbol,org_name`.

## Output Policy Review

- Raw provider rows are not retained.
- Credentials, API keys, and generated Device-Id values are not retained.
- Evidence keeps command class, row counts, and schema shape only.
