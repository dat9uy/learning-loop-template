# Web Stack Capability Scripts

This directory is reserved for TanStack Start / TypeScript capability scripts.

Allowed probes include frontend integration checks, route loader feasibility checks, client SDK surface checks, and rendering-boundary checks. Follow `docs/operator-guide.md` → "Stacks and Capability Locations" before adding files here.
