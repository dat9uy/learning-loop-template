# Product

This directory stays empty until an approved build experiment chooses a product surface, stack, and validation path.

The first approved learning-loop experiment is proposal-only and must not create application code.
